import styled from "@/components/footer.module.css"
import Index from "@components/Logo";
export default function Footer(){
    return (
        <footer className={styled.footer}>
            <Index/>
        </footer>
    );
}